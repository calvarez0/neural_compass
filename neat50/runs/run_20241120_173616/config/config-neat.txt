[NEAT]
fitness_criterion     = max
fitness_threshold     = 1000000000
pop_size             = 150
reset_on_extinction  = True

[DefaultGenome]
# node activation options
activation_default      = tanh
activation_mutate_rate = 0.2
activation_options     = tanh sigmoid relu

# node add/remove rates
node_add_prob          = 0.5
node_delete_prob       = 0.2

# network parameters
num_hidden              = 4
num_inputs             = 17
num_outputs            = 2
initial_connection     = full_direct
feed_forward          = False

# connection add/remove rates
conn_add_prob          = 0.5
conn_delete_prob       = 0.2

# connection enable options
enabled_default         = True
enabled_mutate_rate    = 0.2

# node bias options
bias_init_mean          = 0.0
bias_init_stdev         = 1.0
bias_max_value          = 30.0
bias_min_value          = -30.0
bias_mutate_power       = 0.5
bias_mutate_rate        = 0.7
# keeping replace rates 0 for now
bias_replace_rate       = 0.1

# connection weight options
weight_init_mean        = 0.0
weight_init_stdev       = 1.0
weight_max_value        = 30
weight_min_value        = -30
weight_mutate_power     = 0.5
weight_mutate_rate      = 0.8
weight_replace_rate     = 0.1

# node response options
response_init_mean      = 1.0
response_init_stdev     = 0.0
response_max_value      = 30.0
response_min_value      = -30.0
response_mutate_power   = 0.4
response_mutate_rate    = 0.4
# going to keep this at 0 to maintain stable learning (or try)
response_replace_rate   = 0.0

# node aggregation options
# how a node combines the inputs it receives from its incoming connections 
# before passing the result through the node's activation function.
aggregation_default     = sum
aggregation_mutate_rate = 0.2
aggregation_options     = sum product min max mean

# genome compatibility options
compatibility_disjoint_coefficient = 1.0
compatibility_weight_coefficient = 0.5

[DefaultSpeciesSet]
compatibility_threshold = 2.75

# double or halve each value to test

# elitism of entire species
# even though we kill a species after 20 generations of stagnation
# we preserve the top 2
[DefaultStagnation]
species_fitness_func = mean
max_stagnation      = 20
species_elitism     = 2

# elitism of individuals for entire population
# top 20% of individuals in a species is allowed to reproduce
# other 80% gets discarded
# I think the reason we have 300 species at first even though the population size is 150
# is because min_species_size is 2 so we double need 2 genomes for every species regardless
[DefaultReproduction]
elitism            = 2
survival_threshold = 0.2
min_species_size   = 2